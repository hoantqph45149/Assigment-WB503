<nav class="navbar navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand text-white" href="#">HL Shop</a>
    <button
      class="navbar-toggler text-white bg-white"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon text-white"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a
            class="nav-link active text-white"
            aria-current="page"
            href="/api/client"
            >Home</a
          >
        </li>
        <li class="nav-item">
          <a
            id="registerLink"
            class="nav-link active text-white"
            aria-current="page"
            href="/api/auth/formregister"
            >Register</a
          >
        </li>
        <li class="nav-item">
          <a
            id="loginLink"
            class="nav-link active text-white"
            aria-current="page"
            href="/api/auth/formlogin"
            >Login</a
          >
        </li>
        <li class="nav-item">
          <a
            id="userLink"
            class="nav-link active text-white"
            aria-current="page"
            href="#"
          ></a>
        </li>
        <li class="nav-item">
          <a
            id="logoutLink"
            class="nav-link active text-white"
            aria-current="page"
            href="#"
            >Logout</a
          >
        </li>
      </ul>
      <form class="d-flex" role="search" action="/api/client/search">
        <input
          id="q"
          name="q"
          class="form-control me-2"
          type="search"
          aria-label="Search"
          placeholder="Tìm kiếm ..."
        />
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>

<script>
  const user = JSON.parse(localStorage.getItem("user"));

  if (user && user.user) {
    document.getElementById("registerLink").style.display = "none";
    document.getElementById("loginLink").style.display = "none";

    document.getElementById(
      "userLink"
    ).innerText = `Hello ${user.user.username}`;

    document.getElementById("logoutLink").style.display = "block";
  } else {
    document.getElementById("registerLink").style.display = "block";
    document.getElementById("loginLink").style.display = "block";

    document.getElementById("logoutLink").style.display = "none";
  }

  document
    .getElementById("logoutLink")
    .addEventListener("click", function (event) {
      event.preventDefault();
      localStorage.removeItem("user");
      localStorage.removeItem("accessToken");
      window.location.href = "/api/auth/formlogin";
    });
</script>
